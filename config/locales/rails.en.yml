# English keeps only what the app overrides — Rails ships its own `en` defaults
# for everything else, which rails.ca.yml and rails.es.yml have to restate.
en:
  activerecord:
    models:
      tag:
        one: Category
        other: Categories
