<%= page_title t(".title") %>

<%= simple_form_for :session, url: session_path do |form| %>
  <%= form.input :email %>
  <%= form.input :password %>

  <%= form.submit t('sessions.new.title'), class: "btn", id: 'login' %>
<% end %>
