<% page_title t('.title') %>
<%= simple_form_for(:new_session, url: sessions_path,
      html: {class: 'form-vertical', id: 'new_session'}) do |f| %>
  <%= f.input :email %>
  <%= f.input :password %>
  <%= f.submit t('sessions.new.title'), class: "btn", id: 'login' %>
<% end %>
