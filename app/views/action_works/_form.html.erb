<%= simple_nested_form_for action_work,
    html: { multipart: true, class: "with-sortable-fields" } do |f| %>
  <%= f.error_notification %>
  <%= f.input :title %>
  <%= f.input :description, as: :ckeditor %>
  <%= f.input :tag_tokens, label: "Tags:",
    input_html: { :'data-locale' => I18n.locale,
                  :'data-preload' => action_work.tags.to_json } %>
  <fieldset class="sortable-fieldset extra-content-fields">
    <legend>Seccions extra</legend>
    <%= f.fields_for :sections %>
    <p><%= f.link_to_add "Add a section", :sections, class: "btn" %></p>
  </fieldset>
  <fieldset class="sortable-fieldset images">
    <legend>Imatges i vídeos</legend>
    <%= f.fields_for :images %>
    <p><%= f.link_to_add t('add_image_or_video'), :images, class: "btn" %></p>
  </fieldset>
  <%= f.input :place %>
  <%= f.input :realization_date %>
  <%= f.input :published %>
  <%= f.button :submit %>
<% end %>
