<%= simple_nested_form_for action_work, 
    html: { multipart: true, class: "with_sortable_fields" } do |f| %>
  <%= f.input :title %>
  <%= f.input :intro, as: :ckeditor %>
  <%= f.input :description, as: :ckeditor %>
  <%= f.input :tag_tokens, label: "Tags:",
    input_html: { :'data-locale' => I18n.locale,
                  :'data-preload' => action_work.tags.to_json } %>
  <fieldset class="sortable_fieldset">
    <legend>Imatges i vídeos</legend>
    <%= f.fields_for :images %>
    <p><%= f.link_to_add "Add an image", :images, class: "btn" %></p>
  </fieldset>
  <%= f.input :place %>
  <%= f.input :realization_date %>
  <%= f.input :published %>
  <%= f.button :submit %>
<% end %>
