<%= page_title t("#{controller.controller_name}.works_list.title") %>
<% page_metadata description: t("meta.descriptions.#{controller.controller_name}") %>

<div class="works-list">
  <%= render partial: 'works/work_preview', collection: @works, as: :work %>
</div>
