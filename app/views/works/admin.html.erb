<ul id="works" class="sortable-list" data-resource="works" data-action="sort" data-order="DESC">
  <% works.try(:each) do |work| %>
    <li id="work_<%= work.id %>">
      <span class="handle" style="width: 50px; height: 50px;">X</span><%= link_to work.title, work %></li>
      </li>
  <% end %>
</ul>
